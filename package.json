{
    "name": "nextjs-item-managment-system",
    "private": true,
    "scripts": {
        "client:dev": "cd apps/web && npm run dev",
        "client:build": "cd apps/web && npm run build",
        "server:dev": "cd apps/api && npm run dev",
        "dev": "concurrently \"npm run client:dev*\" \"npm run server:dev\""
    },
    "workspaces": [
        "apps/*"
    ],
    "dependencies": {
        "concurrently": "^9.0.1",
        "typescript": "^5.6.3"
    }
}
